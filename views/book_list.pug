extends layout 

block content
  h1= title 
  if book_list.length 
    ul 
      for book in book_list 
        li
          a(href=book.url) #{book.title}
          | (#{book.author.name})
  else 
    p There are no books.