extends layout 

block content 
  h1= title 
  

  if author_list.length 
    ul 
      for author in author_list 
        li 
          a(href=author.url) #{author.name}
          | (#{author.lifespan})

  else 
    p There are no authors referenced in this library.